# Port the Express backend listens on.
PORT=4000

# Which origin is allowed to call this API (CORS). Must match wherever
# the frontend is actually served from. In local development with two
# separate dev servers, this is Vite's default port.
CORS_ORIGIN=http://localhost:5173